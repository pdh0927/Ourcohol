import 'package:flutter/material.dart';

class Hangover extends StatelessWidget {
  const Hangover({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: const Text('숙취해소 페이지')));
  }
}
