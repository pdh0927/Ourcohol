import 'package:flutter/material.dart';

class TalkTopics extends StatelessWidget {
  const TalkTopics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: const Text('토크 주제 페이지')));
  }
}
